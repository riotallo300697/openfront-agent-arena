# Agent Rules

This document will describe the rules for agents that play in OpenFront Agent Arena.

Current status: placeholder for a future stage.

For now, no agent rules are finalized because we have not yet researched the OpenFront core or defined the Agent API.

Expected future topics:

- what information an agent can see;
- what actions an agent can send;
- action limits;
- timeouts;
- invalid action handling;
- fair play rules;
- replay and audit behavior.

